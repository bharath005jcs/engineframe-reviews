{
  "name": "engineframe-reviews",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Self-hosted Google Reviews widget for Engine Frame Studios",
  "scripts": {
    "seed": "node scripts/seed.mjs",
    "refresh": "node scripts/refresh.mjs",
    "build": "node scripts/build.mjs",
    "dev": "npm run build && npx serve dist -p 3000 -L",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.33.1",
    "dotenv": "^16.4.7"
  },
  "devDependencies": {
    "wrangler": "^4.85.0"
  }
}
